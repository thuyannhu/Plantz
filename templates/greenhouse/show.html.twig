{% extends 'base.html.twig' %}

{% block title %}Greenhouse{% endblock %}

{% block body %}
    <h1>Greenhouse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ greenhouse.id }}</td>
            </tr>
            <tr>
                <th>Light</th>
                <td>{{ greenhouse.light }}</td>
            </tr>
            <tr>
                <th>Humidity</th>
                <td>{{ greenhouse.humidity }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ greenhouse.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_greenhouse_index') }}">back to list</a>

    <a href="{{ path('app_greenhouse_edit', {'id': greenhouse.id}) }}">edit</a>

    {{ include('greenhouse/_delete_form.html.twig') }}
{% endblock %}
